import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Contact",
    description: "My Contact App",
  };

export default function ContactPage(){
    return (
        <div>
            Contact Page
        </div>
    );
}
